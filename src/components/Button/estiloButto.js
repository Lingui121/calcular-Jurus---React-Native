import { StyleSheet } from "react-native";

 const styles = StyleSheet.create({
    textButton:{
        backgroundColor: "red",
        padding:13,
        borderRadius:25,
        textAlign:"center",
        color: "white",
        fontWeight:"bold",
        fontSize:20
    },

})
export default styles